<UserControl x:Class="WpfApp1.View.FilesSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.View"
             mc:Ignorable="d" 
             Height="30" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Border Background="DarkGray" BorderThickness="1">
            <TextBlock Background="Transparent"
                Text="Edit Folder"
                FontSize="19"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        </Border>
        <Border Grid.Column="1" Background="DarkGray" BorderThickness="1">
            <Button Background="Transparent"
                Content="?"
                Width="30"
                FontSize="19"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderThickness="0"
                Command="{Binding OpenSelectedFolder}"/>
        </Border>
    </Grid>

</UserControl>
